version: 2

models:
  - name: dim_products
    description: "Product dimension table with metrics"
    columns:
      - name: product_id
        description: "Primary key for products"
        tests:
          - not_null
          - unique
      - name: total_revenue
        description: "Total revenue from product sales"
        tests:
          - not_null
